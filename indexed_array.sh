#!/bin/bash 

fruits=("apple" "banana" "cherry")

echo ${fruits[0]}
echo ${fruits[1]}
echo ${fruits[2]}



# an array is a variable that is going to store multiple values instead of just one 
#
# this example is representing an indexed array which is storing multiple values by numberic index 
# start from 0 (index[0])

